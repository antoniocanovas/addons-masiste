<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="repairorder_products_report" model="ir.actions.report">
            <field name="name">Presupuesto Reparación productos</field>
            <field name="model">repair.order</field>
            <field name="report_type">qweb-pdf</field>
            <field name="report_name">repair.report_repairorder_products</field>
            <field name="report_file">repair.report_repairorder_products</field>
            <field name="print_report_name">( object.state == 'draft' and 'Presupuesto de Reparación - %s' % (object.name) or 'Orden de Reparación - %s' % (object.name))</field>
            <field name="binding_type">report</field>
        </record>

    </data>
</odoo>