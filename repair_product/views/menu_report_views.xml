<?xml version='1.0' encoding='UTF-8'?>
<odoo>

    <record id="repair_without_tables_report" model="ir.actions.report">
        <field name="name">Presupuesto / Pedido Reparación Cliente</field>
        <field name="model">repair.order</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">repair_product.repair_for_client_report_document</field>
        <field name="report_file">repair_product.repair_for_client_report_document</field>
        <field name="print_report_name">( object.state == 'draft' and 'Presupuesto de Reparación - %s' % (object.name) or 'Orden de Reparación - %s' % (object.name))</field>
        <field name="binding_type">report</field>
    </record>

</odoo>